[pytest]
# The importer code now lives in src/. Tests stay in the root tests/ folder and
# import modules by top-level name (import main, from csv_parser import CSVParser,
# import utils.general_utils, ...). Putting src/ on sys.path makes those imports
# resolve without any per-test path hacks.
pythonpath = src
testpaths = tests

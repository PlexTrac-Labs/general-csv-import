"""Reusable helpers for building source-specific mapping types.

Modules in this package hold logic that is shared across mapping types but is
not part of the generic temp-CSV-to-PTRAC engine in ``csv_parser.py``.
"""
